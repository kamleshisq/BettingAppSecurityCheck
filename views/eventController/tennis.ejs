<div id="TENNIS" class="tabcontent">
  <div class="row">
    <section>
      <div class="container-fluid">
        <div class="table-data-section">
          <div class="data-table">
            <table>
              <thead>
                <tr>
                  <th>S.No</th>
                  <th>Enabled</th>
                  <th>Date</th>
                  <th>Team</th>
                  <th>Open Bet</th>
                </tr>
              </thead>
              <tbody class="new-body">
                <% for(let i = 0; i < data.tennisEvents.length; i++){%>
                <%if((i%2)==0){%>
                <tr>
                  <%}else{%>
                  <tr>
                    <%}%>
                    <td><%=i + 1%></td>
                    <td>
                      <%if(data.tennisEvents[i].eventData.status){%>
                      <div class="on-off-btn-section">
                        <span class="on-off">OFF &nbsp; <label class="switch">
                        <input class="checkbox status_check" name="autoSattled" type="checkbox" data-id="<%=data.tennisEvents[i].eventData.eventId%>" checked>
                        <span class="slider round"></span>
                        </label>&nbsp; ON</span>
                      </div>
                      <%}else{%>
                      <div class="on-off-btn-section">
                        <span class="on-off">OFF &nbsp; <label class="switch">
                        <input class="checkbox status_check" name="autoSattled" type="checkbox" data-id="<%=data.tennisEvents[i].eventData.eventId%>">
                        <span class="slider round"></span>
                        </label>&nbsp; ON</span>
                      </div>
                      <%}%>
                    </td>
                    <%
                      var timestamp = data.tennisEvents[i].eventData.time * 1000 
                      var date = new Date(timestamp);
                      var options = { 
                          year: 'numeric',
                          month: 'long',
                          day: 'numeric',
                          hour: 'numeric',
                          minute: 'numeric',
                          hour12: true
                      };
                      var formattedTime = date.toLocaleString('en-US', options);
                    %>
                   
                    <td><%=formattedTime %></td>
                    <td><%=data.tennisEvents[i].eventData.name%></td>
                    <td><%=data.tennisEvents[i].eventData.count%></td>
                    <%}%>
                  </tr>
                </tr>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </section>
  
  </div>
  <!-- <button class="btn btn-view-all">View All</button> -->
</div>